# TESTING.md — Testing Rules

---

## Commands

```bash
npm test              # run all tests
```

---

## Rules

- **Never remove a failing test.** Fix it or escalate.
- **New features need tests.** Any new business logic must have a corresponding test.
- Tests must pass before a PR is marked ready for review.
